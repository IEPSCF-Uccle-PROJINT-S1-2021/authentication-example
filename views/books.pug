extends layout

block content
  h1= title
  table.table
    thead
      tr
        th(scope=col) Author
        th(scope=col) Title
    tbody
      each book in books
        tr
          td= book.author
          td= book.title
